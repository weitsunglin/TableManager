export enum ID {
    // 自行新增 ID KeyValue
}

export enum ColumnName {
    ID = 'ID',
    String = 'String',
}

class TestData2Extend {
    constructor() {
    }
}

const testdata2extend = new TestData2Extend();
export default testdata2extend;